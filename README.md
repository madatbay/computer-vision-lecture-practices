# Computer Vision lecture practices

## Set up your env

Create and activate `venv`:

```bash
python3 -m venv venv
source venv/bin/activate
```

Install dependencies:

```bash
pip3 install -r requirements.txt
```
